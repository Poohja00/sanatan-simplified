export default function ContactPage() {
  return (
    <section className="mx-auto max-w-[700px] px-[6vw] py-24">
      <div className="text-[11px] font-semibold uppercase tracking-[0.2em] text-vyoma-gold">
        Contact
      </div>
      <h1 className="mt-4 font-serif text-[36px] font-medium text-vyoma-ink">
        Get in touch.
      </h1>
      <p className="mt-4 text-[15px] leading-[1.7] text-vyoma-muted">
        A dedicated contact channel is on the way. Until then, use the
        newsletter signup in the footer to stay in the loop as VYOMA grows.
      </p>
    </section>
  );
}
